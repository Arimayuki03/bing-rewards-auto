# 改动与待办记录

> 项目：`微软积分商城签到（全能智能重构版）.user.js`
> 本文件记录**已完成改动**与**未完成/评估后不做**的任务。第一轮详细分析见 [`优化分析.md`](优化分析.md)。
> 更新日期：2026-09-14　当前版本：v3.6.8　测试：72/72 通过

---

## 〇-H、v3.6.8（第十一轮）— 前台同源转发通道：治 Server Action 500 / PC 签入 401

**背景**：v3.6.7 运行锁修复生效，21:40 轮后台完整跑通（不再"本轮跳过"）。同轮日志暴露下一层：
每日活动 3/3 全部 `Server Action context 失败 HTTP 500 digest @E80`，5 张卡片"所有策略均失败"，
PC 签入 401、"PC会话已过期"、getuserinfo 不可用——而显式 Cookie 头已确实随请求发出
（debug dump `cookies: 26~27`）。这正是 v3.6.6 判定要点预告的分叉：SW/sandbox 子请求的
cookie 链不全（GM_cookie 按 URL 拉取是子集）或被 ScriptCat 底层剥头，只有页面上下文的
同源 fetch 能带上浏览器真实的完整 cookie（v3.6.5 curl 重放已证明 payload+全量 cookie=200）。

| 类别 | 改动 | 说明 |
|---|---|---|
| P1 | 前台同源转发通道（`Utils.xhr` 唯一咽喉集成） | 页面侧（所有 rewards.bing.com 页）挂 20s 心跳 `BingRewards_alive` 并监听 `BingRewards_req`，用页面 `fetch(credentials:include)` 执行同源 GET/POST，回包 `BingRewards_resp`；后台侧 `rewards.bing.com/*` 请求优先走通道，非 2xx 的 acceptErrorBody/抛错语义与直连一致（`HTTP 401（同源转发）` 等字样保留，上层 401/400 判定不受影响） |
| P1 | 无前台时自动救援 | 首轮 rewards 请求时若无活页面，开一个隐藏标签页 `dashboard?bgprobe=1`（bgprobe 页只挂通道、**跳过** autoClaim/autoClickDailySet 等 DOM 自动处理，避免导航打断转发），等心跳 ≤25s；失败则本轮禁用通道回退直连；轮末由 runAll finally 关闭自开的标签页（用户自己的标签页不关）。转发失败/超时一律静默回退直连，通道只可能改善、不可能恶化 |
| P2 | `Config.pageProxy` 开关（默认开） | UserConfig 新增复选框，出问题可一键回退纯直连 |
| P2 | `isBackground` 判定修正（renewToken + init 延迟两处） | 日志证据："⏳ 54.731秒后启动 {env:service_worker}"——ScriptCat crontab 在带 document 的 sandbox 页执行，`typeof document` 判定把后台误判为前台（5-95s 延迟、无谓的自动授权 fetch 尝试）；改为"非 bing 系主机名即后台" |
| P2 | 测试 65 → 72 | 通道命中/非 2xx 语义/页面执行失败回退/救援不可用即时禁用不挂起/搜索域名不路由、页面接线与 bgprobe 旁路（源码结构断言）、isBackground 双处修正 |

**判定要点（下一轮日志）**：
- `每日活动完成 3/3`、卡片 `所有策略均失败` 消失、PC 签入不再 401 → cookie 链根因坐实、修复完成；
- 若出现 `🔗 已打开后台代理标签页` 但随后 `🟡 后台代理标签页未就绪` → 代理页被重定向到登录或
  ScriptCat 未给该 URL 注入（检查 @match 是否覆盖、扩展模式）；
- 若通道已命中仍 500/401（`HTTP 500（同源转发）` 字样）→ cookie 之外还有变量（如 next-action ID
  轮换失效、payload 与页面当前构建不匹配），届时用同 id 对比页面真实请求头再定位。

**验证**：`node --test tests/userscript.logic.test.cjs` 72/72 通过；`node --check` 语法 OK。
转发通道端到端行为依赖真实浏览器 fetch，harness 以预置回包/桩 GM 验证后台侧全部分支；
页面侧接线用源码结构断言锁定。

---

## 〇-G、v3.6.7（第十轮）— 运行锁加固：卡死实例不再把后台永久锁死

**背景**：用户日志显示 service_worker 每轮都报"检测到另一脚本实例正在运行（运行锁），本轮跳过"，
脚本完全停摆。根因是 v3.6.6 心跳续期的设计盲区：心跳只能证明"持有实例还活着"，证明不了
"任务还在推进"。一个 await 永不返回的卡死轮次（SW 挂起/恢复后 promise 悬挂等）每 5 分钟
把锁续期一次，锁永远"有效"，此后每一轮都被挡掉——v3.6.5 时代最多停摆 60 分钟，v3.6.6 反而
变成永久停摆。另外损坏/超大 expire 的锁记录同样无解。

| 类别 | 改动 | 说明 |
|---|---|---|
| P1 | 锁记录扩展为 `{token, expire, lastBeat, acquiredAt}`；接管判据三条 | ① lastBeat 失联 >3 个心跳周期（15 分钟）→ 持有者已死/计时器冻结，接管；② 总持有 >90 分钟（约最慢合法轮次 3 倍）→ 判定卡死，接管；③ expire 超前 >2 个过期窗口 → 视同损坏锁，接管 |
| P1 | `_renewRunLock` 返回值化 + 心跳计时器自停 | 续期被拒（被接管或达上限）时 runAll 的心跳 `setInterval` 自行 `clearInterval`，卡死轮次停止续锁；上限内不改写锁记录，交锁自然过期 |
| P1 | 跳过/接管时输出持有者画像 | `持有者 xxxxxxxx…，心跳 N 秒前，已持有 M 分钟，锁剩余 K 分钟`——下次再出现"一直跳过"可直接从日志判断占锁者是谁、是死锁还是活实例（后者要查是否重复安装了脚本） |
| P2 | 旧记录兼容 | 无 lastBeat/acquiredAt 的 v3.6.6 锁按 `expire - 20min` 回推心跳时刻、续期时补齐新字段，升级平滑无需清存储 |
| P2 | 测试 62 → 65 | 三条接管判据 + 两个不误抢对照、续期补戳/上限拒续/不触他人锁、runAll 心跳达上限自停；harness 的 `setInterval` 桩改为保留回调以便手动触发心跳 |

**验证**：`node --test tests/userscript.logic.test.cjs` 65/65 通过；`node --check` 语法 OK。

**用户侧注意**：若日志显示占锁者心跳始终新鲜（活实例在跑完整轮次），说明是真实的并发实例在运行。
（初版本文曾据"日志 uuid 与工作区导出 uuid 不一致"怀疑重复安装，v3.6.8 轮日志证伪：
同一次安装的运行 uuid 也在变（4dca7182… → 7f2e8b0d…），uuid 是每次执行的实例标识，不是脚本身份。）

**后续验证（2026-09-14 21:40–21:48 轮日志）**："每轮跳过"未再出现，后台完整执行了一整轮
（Token→签入→阅读→搜索 6 次→连签→每日活动→打卡→卡片→二次扫描），运行锁修复确认生效。

---

## 〇-F、v3.6.6（第九轮）— 代码审查修复：卡片领取策略1补 Cookie 头、前台打卡日期基准、运行锁心跳

**背景**：2026-09-14 全量代码审查（主脚本 4214 行 + 测试 + 文档，58/58 测试基线）发现 3 个
P2 问题，本轮全部修复。v3.6.5 的每日活动 500 修复仍待下一轮运行日志按〇-E 判定要点确认。

| 类别 | 改动 | 说明 |
|---|---|---|
| P2 | `claimCard` 策略1（手写 reportactivity POST）补显式 Cookie 头 | v3.6.5 四处修复遗漏的首选路径：同根因（SW 子请求缺 SameSite 登录 cookie）下策略1 必然失败，每次领卡先白打一次请求再落到策略2/3；现与 `API.reportActivity` 同款挂 `cookieHeaderFor` |
| P2 | 前台 rewards 块开头初始化 `state.dateNowNum/dateNowStr` 为真实日期 | /dashboard 分支在后台入口 `init()` 之前 return，`TaskManager.init()` 不执行，`clickPunchCards` 原以 `dateNowNum=0` 读写 `punchCardDate/Attempts` 等日期键——打卡状态机与"每日尝试上限 3 次"在 dashboard 与其他 rewards 页面两族间各自独立、互相重置（入口最多 ~2× 点击）。现在两族共用同一日期键 |
| P2 | 运行锁默认过期 60 分钟 → 20 分钟 + 运行中心跳续期（每 5 分钟，`_renewRunLock`） | 实例中途被杀（关标签页/SW 回收）后无人释放锁，原需等满 60 分钟后台 crontab 才恢复，期间全部轮次跳过；现在心跳停止后 20 分钟自动让出，活跃长任务（含最大搜索间隔配置）由心跳保活不受影响 |
| P2 | 测试 58 → 62 | 策略1 Cookie 头、前台日期初始化（源码结构断言）、锁 20 分钟过期与续期归属、心跳随 runAll 启停；harness 补 `setInterval/clearInterval` 桩并暴露 `intervals` 记录 |
| P3 | 顺带：`@description` 版本注记从滞留的 v3.6.3 刷新到当前轮 | 头部元数据与实际版本对齐 |

**验证**：`node --test tests/userscript.logic.test.cjs` 62/62 通过；`node --check` 语法 OK。
修复2 涉及前台 DOM 流程，harness 无法运行前台块（`location.hostname=test.invalid`），
采用源码结构断言锁定初始化位置，行为正确性经代码审查确认（dashboard 上 `init()` 确不执行、
非 dashboard 页面 `TaskManager.init()` 会以相同值覆盖，无行为差异）。

---

## 〇-E、v3.6.5（第八轮）— 决定性定位：500 的根因是 SameSite cookie 缺失，修复已实测验证

**决定性证据**（用户提供真实浏览器点击卡片的 curl，本地重放）：
1. 原样重放浏览器请求（context 形状 payload + flight hash + 全套浏览器头+cookie）
   → **HTTP 200，action 返回 `1:true`**；
2. 二分矩阵（Child2/Child3 逐项验证）：**V1 = 脚本现有最小头集合 + 浏览器 cookie
   同样 200**。x-deployment-id、sec-fetch-*、sec-ch-ua、UA 版本（131/153）、
   priority/dnt/x-client-data 全部无关；
3. 结论：payload 自 v3.6.2 起就是正确的，唯一变量是 **cookie**——ScriptCat SW
   发起的跨源子请求不自动携带 `SameSite=Lax/Strict` 的登录 cookie
   （`.MSA.Auth`/`_U`/`rn_S`/`_RwBf` 等），服务端 action 鉴权链不完整抛错 →
   固定 digest 的 500。curl 不校验 SameSite，所以带 cookie 的 curl 能过；
4. 副产物：重放已把 Child1/2/3 全部入账（今日每日活动 +30p 实测成功）。

> 顺带修正此前两个误判：① v3.6.2 的 context 形状本来就是入账路径，"改形状"
> 方向错了；② 浏览器点击时 reportActivity 正常返回 200，并不存在"500 也不妨碍
> 入账"的假设——入账就是它完成的。

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | 新增 `Utils.cookieHeaderFor(url)`：GM_cookie（`"list"`，ScriptCat 已 grant）读全量 cookie（含 httpOnly），拼成显式 `Cookie` 头；不可用/超时（2.5s）返回 "" | GM_cookie 无视 SameSite 读取；显式头补齐 SW 缺失的登录 cookie |
| P0 | `_sendDailySetAction` / claimCard 策略 2 / `API.reportActivity`（legacy）/ `_getUserInfo`（getuserinfo）四处请求挂显式 Cookie 头 | 同根因的四个 500/401 端点一次修复；PC 签入 401、getuserinfo 不可用有望一并恢复 |
| P2 | debug dump 增加 `cookies: N` 字段 | 下一轮可直接确认 GM_cookie 是否生效 |
| P2 | 测试 55 → 58（cookieHeaderFor 构造/不可用回退、_sendDailySetAction 挂头、reportActivity 挂头） | harness 默认 GM_cookie 同步回调空列表 |

**判定要点（下一轮日志）**：
- 出现 `每日活动完成 3/3` / `Server Action ... 200` → 修复完成；
- 若仍 500 且 debug dump 里 `cookies: 0` → GM_cookie 在 SW 不可用（检查 ScriptCat
  版本/权限），需另想办法取 cookie；
- 若 `cookies: 40+` 仍 500 → ScriptCat 底层丢弃了显式 Cookie 头，下一步改为
  前台页面上下文直发（rewards.bing.com 同源 fetch，浏览器自动带全量 cookie）。

**安全提示**：调试用的 curl 含完整登录态 cookie，仅本地使用后已删除；请勿把
这类内容粘贴给不受信任的第三方。

---

## 〇-D、v3.6.4（第七轮）— 确认 v3.6.3 已生效仍 500；改挖"点击跳转"入账路径 + 变体阶梯

**故障背景**（2026-09-14 03:20 运行日志，v3.6.3 已确认在跑）：payload 已是
`["hash",11,{"offerid","form":"$undefined"}]`，action ID 经动态重扫验证
（`动态 reportActionId(dpl=20260912-2): 707e6eb15bdf…`，与缓存一致），**仍然全部
HTTP 500**（0.14-0.21 秒快速失败）。至此已试过 4 种组合全 500：
flight hash × 两种形状、flyout hash × 两种形状。

**本轮离线新证据**（匿名拉取当前部署全部公开 JS chunk 重新逆向）：
1. 确认只有 `reportActivity`/`submitEmailConsent` 两个带名 action，ID 一致；
2. 真实调用点共三类，其中**卡片组件点击入账**（`ActivityCardAction` +
   `ReportActivityContext`）的形状此前从未精确复刻过：
   `reportActivity(卡片hash, 卡片type ?? 11, {offerid, isPromotional: toString(), timezoneOffset})`
   ——`hash` 与 `type` 都来自**服务端下发的卡片 props**；
3. DailySet 侧栏（`DailySetSidePanel`）是**纯链接组件，不上报**——bingredirect
   类活动的入账发生在**点击跳转的目标页（rnoreward=1）**，与搜索配额同源；
   reportActivity 只是卡片可见/点击时的伴随上报，在真实浏览器里它自己 500 也
   不妨碍入账（脚本从未验证过浏览器端这些调用是否成功）；
4. 脚本 flight 解析此前对同一 offerId **首个命中即停**——若 dashboard flight 里
   还存在带 `type`/`form` 的富对象/印象对象，全部被漏掉（日志中 flight offer dump
   恰好只有瘦字段，无法排除这种可能）。

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | `_extractDailySetHashes` 收集同一 offerId 的**全部** flight 对象为 variants（去重），首选带 `form` 的变体；debug 下逐个 dump | 不再"首个命中即定终身"，富对象（type/isPromotional）与印象对象（form）不再丢失 |
| P0 | doDailySet 改为**三段阶梯**：① impression 形状（仅当变体带真实 form）→ ② context 形状（卡片点击调用点，`type ?? 11`）→ ③ **后台 GET 活动目标链接**（复刻真实点击的 rnoreward 跳转入账） | 阶梯 ③ 是 bingredirect 类活动最可能的真入账路径，此前只有 hash 提取完全失败才能走到打开链接兜底 |
| P0 | `_sendDailySetAction` 形状参数化（impression/context 两种 payload 均按 bundle 复刻）；请求带 `acceptErrorBody`，**非 2xx 时直接记录状态+响应体（digest）** | 修复 v3.6.3 运行日志里 `Server Action 响应` dump 偶发丢失的问题（原调试通道靠二次重发，会静默失败） |
| P1 | claimCard 策略 2 补 `accept: text/x-component` 头；新增**策略 5**：GET 卡片目标链接（bing.com 域）复刻点击 | 与每日活动阶梯 ③ 同源；策略 4 从"最终兜底"改为继续尝试 |
| P2 | 新增 `TaskManager._visitActivityUrl`（后台访问活动链接公用助手，bing.com 域校验） | claimCard（API 对象）经 `TaskManager._visitActivityUrl` 调用 |
| P2 | 测试 51 → 55 | 变体收集/去重/首选 form、context 形状 payload 与 500 body 记录、doDailySet 阶梯③、claimCard 策略 5 |

**判定要点（下一轮日志）**：
- `Server Action context/impression 失败(...): HTTP 500: <响应体>` ——响应体现在
  **必定出现**，对比 digest 是否仍为 `3200825472@E80`：变了说明参数起效、失败点后移；
- 若出现 `已后台访问活动链接(...)` 且复查后 `每日活动完成 3/3` ——点击跳转路径打通；
- **决定性对照实验（需手动）**：登录浏览器打开 rewards.bing.com/dashboard，F12 →
  Network 过滤 `dashboard`，点击一张未完成的每日活动卡片，看伴随的
  `reportActivity` POST 是否也 500：若浏览器也 500 而**积分正常入账**，则证明
  Server Action 与入账无关、脚本阶梯 ③ 即正解；若浏览器 200，抓它的请求头+payload
  （hash/type/form 三字段）与脚本日志对比即可锁定剩余偏差。

---

## 〇-B、v3.6.2（第五轮）— Server Action HTTP 500 修复

**故障背景**（2026-09-13 19:18 运行日志）：v3.6.1 的链路全部打通（`动态 reportActionId` ✓、
`flight 流解析到 3 个每日活动 hash` ✓、flyout 回退 ✓），但登录态的 Server Action 上报
返回 **HTTP 500**（0.14-0.26 秒快速失败）。

**判别实验**（匿名 curl 实测 `/dashboard` + `next-action` 头）：

| 变体 | 结果 | 结论 |
|---|---|---|
| 垃圾 action ID | 404 "Server action not found." | 脚本解析出的 ID 正确 |
| 非 JSON body | 500 + `1:E{"digest":…}` | 500 = 服务端解码/执行抛异常 |
| 有效形状 + 假 hash + 无会话 | 200（RSC 登录重定向） | 匿名时中间件先拦截，action 不执行；500 只发生在登录态执行阶段 |

对照 v4 与真实浏览器行为发现两处 payload 偏差，均按浏览器实际行为对齐：

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | `timezoneOffset` 改用 JS 原始符号（中国区 "-480" 而非 "480"） | 新版 action 的该字段来自浏览器 client 的 `new Date().getTimezoneOffset()`（v4 同款：`String(new Date().getTimezoneOffset())`）；旧约定（东经为正）仅保留给 legacy DAPI/cn.bing.com 上报（`Utils.getTimezoneOffset` 不变，新增 `Utils.jsTimezoneOffset`） |
| P0 | Router-State-Tree 叶子标记 `PAGE` → `__PAGE__` | 真实浏览器协议使用 `__PAGE__`；v4 自身的 quest 树也用 `__PAGE__`（dashboard 树疑似笔误）。若新构建校验树结构，旧标记会 500 |
| P1 | claimCard 策略 2 补 origin/user-agent 头，timezoneOffset 同步改原始符号 | 与 `_sendDailySetAction` 请求形状完全一致 |
| P2 | 调试日志增强（`Config.debugDailySet`） | ① flight offer 原始字段 dump（含被跳过项）；② Server Action 失败时 dump 完整请求 + 500 响应体（`Utils.xhr` 新增 `acceptErrorBody` 调试通道）；③ flight 兜底列表解析同款 |
| P2 | 测试 49 → 51 | jsTimezoneOffset 符号约定、`__PAGE__` 树标记 |

**若 v3.6.2 仍 500**：开启菜单"调试日志（每日活动）"跑一轮，把 `flight offer:`、
`Server Action 请求:`、`Server Action 响应:` 三类日志发回，即可定位剩余偏差
（例如 flight 对象的 hash 字段并非上报 token 的场景）。

---

## 〇-C、v3.6.3（第六轮）— 从 bundle 反向出真实 payload 形状，修复 HTTP 500

**故障背景**（2026-09-14 01:29 运行日志）：v3.6.2 仍 500，排除时区符号与树标记。

**决定性证据**：把当前部署全部 51 个公开 JS chunk 挖完后确认——
1. 全构建只有两个带名 Server Action：`reportActivity`、`submitEmailConsent`，
   不存在其他"每日活动专用"action，脚本用的 ID 没错；
2. 找到了浏览器端**真实调用点**，共三种形状：
   - **印象上报（卡片可见即入账）/ 活动弹窗关闭**（3 处调用，含 DailySet 侧栏模块）：
     `reportActivity(hash, 11, {offerid, form})` ← **这是真实入账路径，payload 携带 offer 自带的 `form` 字段**
   - Context 包装（卡片组件低频路径，1 处）：`{offerid, isPromotional: x?.toString(), timezoneOffset: "-480"}`
   - 所有调用点 activityType **硬编码 11**；
3. 旧 payload（v4 式 context 形状）缺少 `form`，且用 flight 对象的 activityType 覆盖了 11。

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | payload 改为 `reportActivity(hash, 11, {offerid, form})` | 完全复刻真实浏览器的入账调用形状；`form` 取自 flight offer 对象（缺失时对象文本内查找，再缺失序列化 `"$undefined"`，与浏览器传 undefined 等价） |
| P0 | activityType 固定 11，不再用 flight 对象的字段覆盖 | 所有真实调用点均为硬编码 11 |
| P1 | claimCard 策略 2 / parseCard / normalizeDashboardCard 同步携带 form | 卡片上报与每日活动上报形状统一 |
| P2 | 调试 dump 增加 form 字段 | flight offer dump 现含 `form`，便于确认数据源 |

**验证待办**：安装 v3.6.3 后观察下一轮日志。若仍 500，开启"调试日志（每日活动）"，
把 `flight offer:`、`Server Action 请求:`、`Server Action 响应:` 三类日志发回
（此时请求体已含 form，可对比真实浏览器 Network 面板中点击活动时的 POST payload）。

---

## 〇-A、v3.6.1（第四轮）— v3.6.0 实测日志修复：数据源兜底

**故障背景**（用户提供的 2026-09-13 18:48 运行日志）：v3.6.0 安装后每日活动仍在
"列表获取"一步中止（`每日活动列表获取失败（接口异常）`），新加的 flight hash 解析
根本没执行到。同一轮日志显示：dashboard/earn 页面 GET 正常（能解析"可领取积分"、
activityCards），但 `getuserinfo` 拿不到数据、PC 签到与卡片旧接口报 401——即新版
站点**页面可访问而旧 API 家族被拒**。此外 claimCard 策略 2 用的还是页面残留提取的
next-action（新版下很可能是其他 action 的 ID）。

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | `_getUserInfo` 失败自动回退 **Bing flyout 接口** | `www.bing.com/rewards/panelflyout/getuserinfo?channel=BingFlyout&partnerId=BingRewards`（TheNetsky v4 同款替代数据源），归一化为旧 dashboard 结构（dailySetPromotions/morePromotions/userStatus.counters，大小写计数器统一），每日活动/搜索配额/活动卡片所有消费方同步恢复 |
| P0 | `getDailySetItems` 增加 **flight 流兜底** | 两个接口都不可用时直接解析 dashboard/earn flight 流中的 `Gamification_DailySet*` offer（含完成状态与日期过滤）；flight 数据完全缺失才返回 null 交由重试机制。列表与完成复查两条路径都不再依赖旧接口 |
| P0 | claimCard 策略 2 改用 **动态解析的 reportActivity ID** | 原 `RewardsAuto._nextAction` 在新版下可能提取到页面其他 action 的 ID；现在优先 `API._resolveReportActivityActionId()`（chunk 扫描），action ID 解析器自 TaskManager 移至 API 供两处共用 |
| P1 | claimCard Server Action POST 目标修正 | Server Action 只能发往 rewards 站点页面：card.url 非 `rewards.bing.com/earn|dashboard` 时（如 bing.com 目标页）改发 earn，Router-State-Tree 随之匹配 |
| P1 | `_extractDailySetHashes`/flight 兜底的 offerId 匹配放宽为 `^Gamification_DailySet` 前缀 | 旧 `Child\d+` 模式对部分新版 offerId（如 `Child_0` 带下划线）不命中；旧 RSC 正则兜底保留原模式 |
| P2 | 测试 46 → 49 | flyout 归一化与回退、flight 列表兜底（含"无 flight 数据返回 null"）、claimCard 目标页路由；getuserinfo 单次抓取断言改为按 URL 家族统计 |

**与 v3.6.0 的日志对照**：本轮日志应出现 `getuserinfo 不可用，已回退 Bing flyout 接口`
（或直接成功）、`flight 流解析到 N 个每日活动 hash`、`动态 reportActionId(dpl=…)`。
若 PC 签到仍 401：legacy reportactivity 接口疑似被新版站点废弃，App 签到已覆盖
（日志显示 App 签入确认成功），不影响每日活动与卡片。


## 〇、v3.6.0（第三轮）— 适配 2026-09 站点改版，修复每日活动完成不了

**故障背景**：用户反馈 rewards.bing.com/dashboard 的每日活动无法完成。排查发现微软于
2026-09-12 重新部署了 Rewards 站点（部署 ID `20260912-2`），dashboard 改为新版 UI：
offer 数据改存于页面 `self.__next_f` flight 流（不再有旧 RSC 明文），Server Action ID
随每次部署轮换且不再出现在页面 flight 流中。旧脚本两条依赖同时断裂：hash 提取
（要求恰好 64 位十六进制、`Child\d+` 正则）落空，next-action 动态提取落空后只能用
过期硬编码 ID → Server Action 被服务端拒绝 → 每日活动无法上报。

参考公开项目 [TheNetsky/Microsoft-Rewards-Script v4](https://github.com/TheNetsky/Microsoft-Rewards-Script)（已适配新版 dashboard）确认了新流程，
并与本站当前构建的公开 JS chunk 实测比对后实施以下修复：

| 类别 | 改动 | 说明 |
|---|---|---|
| P0 | 新增 flight 流解析（`concatFlightChunks` + `extractFlightObjects`） | 拼接 `self.__next_f.push([1,"..."])` 分片，字符串感知的花括号配对提取含 `"offerId"` 的 JSON 对象（hash/complete/locked/date/activityType/isPromotional），支持 40-64 位 hash（当前为 40 位 SHA-1） |
| P0 | 新增 `_resolveReportActivityActionId`：从 JS chunk 动态解析 action ID | 收集 /earn、/dashboard 引用的 chunk URL（保留 `?dpl=` 部署参数），扫描 `createServerReference("…", …, "reportActivity")`，命中即停（单轮上限 16 个 chunk） |
| P0 | action ID 跨轮持久缓存（按 dpl） | `Config.reportAction = {dpl, id}`：部署 ID 一致即复用，省去每轮 chunk 扫描；部署更新自动重扫 |
| P0 | `_extractDailySetHashes` 重写 | 主路径：flight offer 与 getuserinfo 的 offerId 精确匹配（跳过已完成/锁定/未来日期）；兜底：旧 RSC 正则（hex 放宽为 40-64 位） |
| P1 | `_sendDailySetAction` 请求对齐新版浏览器行为 | 补 `Next-Router-State-Tree` 头（`Utils.routerStateTree`，'(nav)' 分组 + 刷新标记 4096）；body 使用 offer 实际 `activityType`（默认 11）与 `isPromotional`（真值序列化 `true`，否则 `"$undefined"`） |
| P1 | `claimCard` 策略 2 补 Router-State-Tree 头；两处硬编码 action ID 统一为 `RewardsAuto.fallbackActionId`（更新为当前构建值 `707e6e…`） | 兜底值仍会随微软每次部署失效，仅作动态解析失败的最后手段 |
| P2 | 新增 `Utils.flightDateToNum`（MM/DD/YYYY → yyyymmdd） | flight 对象的 date 与运行起始日同口径比较 |
| P2 | 测试 36 → 46 | flight 拼接/对象提取/action ID 提取/日期换算/hash 匹配过滤/Server Action 请求体与头/dpl 缓存复用与重扫/doDailySet 集成（含"复查绕过缓存"断言） |

**验证方式**：解析策略已对当前部署（20260912-2）的公开页面与 chunk 实测（flight 拼接
55KB、24 个 chunk、`reportActivity` ID 提取均命中）；2.5MB 压力载荷 2ms 内完成解析。
登录态下的实际上报需观察下一轮运行日志（日志关键字：`flight 流解析到 N 个每日活动
hash`、`动态 reportActionId`；异常时开启 `Config.debugDailySet` 查看原始字段）。


## 一、已完成改动

### v3.5.0（第一轮）— 请求缓存层 + 正确性修复

| 类别 | 改动 | 说明 |
|---|---|---|
| P1 | 新增 `Utils.fetchPage`/`fetchJson` 按运行周期缓存 | 同轮内 earn/dashboard/getuserinfo/DAPI `/me` 各只请求一次；失败不缓存；`fresh:true` 逃生门 |
| P1 | 接入全部读路径 | earn 5→1 次/轮、dashboard 5→1（RSC 变体独立缓存）、getuserinfo 3→1、DAPI `/me` 4-5→1（`options=105` 余额并入 `options=613` 同一份响应） |
| P1 | 所有"执行后复查"路径强制 `fresh` | doPromos 复核、二次扫描复核、doDailySet 两处、doSearch 终查、runAll 汇总 |
| P1 | 删除 runAll 双份随机延迟 | doDailySet 前冗余延迟，每轮省 3-8 秒 |
| P0 | 前台处理器改用 `state.dateNowNum` | 消除跨午夜与后台存储键错位导致的重复点击 |
| P0 | `clickDetailTasks` 找不到按钮不再无条件标记完成 | 避免页面未渲染完即误判"今日已完成"，改为有"已完成"证据才标记 |
| P2 | Fisher–Yates 洗牌、`dateParts` 日期工具统一 | 替代有偏的 `sort(()=>Math.random()-0.5)` |
| P2 | 测试从 27 → 35 | 缓存命中/fresh 绕过/日期失效/失败不缓存/RSC 隔离/复核 fresh 断言 |

### v3.5.1（第二轮）— 小修与去重

| 类别 | 改动 | 说明 |
|---|---|---|
| P1 | 修复「完成后停止循环」菜单**提示语反转** | `alert(!keep ? …)` → `alert(keep ? …)`，原提示与操作结果完全相反（见下方"修复过程"） |
| P2 | 新增 `Utils.extractNextAction` | 消除 3 处逐字重复的 next-action 提取正则（discoverCards / 两个 dashboard 提取） |
| P2 | 删除死代码 `Utils.waitForElement` | 全文件仅定义、零调用（保留其"复数版" `waitForElementsByText`） |
| P2 | doSearch 轮内搜索词去重 | 50 词池随机取 4-7 次约 1/4 概率撞词，撞词重试 3 次换词；重复词更可疑且通常不计分 |
| P2 | 测试 35 → 36 | `extractNextAction` 解析 raw/转义两种 payload |

---

## 二、未完成任务（待办）

### 🔴 需要真实账号实验才能确认的

| # | 事项 | 位置 | 现状与风险 |
|---|---|---|---|
| 1 | `reportSearch` 的 GET+POST 双请求 | `API.reportSearch` / `doSearch` | 每次搜索先 GET 搜索页（数百 KB）再 POST 上报。两点存疑：① 若 POST 上报即计分，GET 纯属浪费流量；② 若只有 GET 计分，POST 冗余。**无法离线判定**——需要真实账号分别关闭 GET/POST 各跑一天对比积分。涉及防风控，不自行改。 |

### 🟡 可做但收益/风险比不高的

| # | 事项 | 现状与风险 |
|---|---|---|
| 2 | 授权码 90 秒轮询改 `GM_addValueChangeListener` | 当前每秒读一次 `Config.code` 可正常工作，轮询成本可忽略；改监听需处理注销与跨引擎兼容，收益小 |
| 3 | `Utils.tryJSON` 消除 `isJSON`+`JSON.parse` 双解析 | 约 8 处调用，每处 `if (isJSON) parse`，改为单次解析返回 null。纯微优化，改动面大 |
| 4 | HTML 清理链统一 | 各处 `replace(/\\"/g,'"')`、`\\u0026`、`&amp;` 组合不完全一致，统一有改变 token/hash 解析结果的风险 |
| 5 | 过期 `Config.*` 存储键清理 | 多数键按日期自我重置（date 字段不匹配即重建），运行锁自带过期，无实际膨胀问题 |
| 6 | `checkRegion` 递归改循环 | 深度上限 2，无栈风险，可读性相当 |

### ⚪ 评估后不做（有意保留现状）

| # | 事项 | 原因 |
|---|---|---|
| 7 | `getRewardsInfo` 300+ 行解析拆分 | 正则/括号匹配各有历史原因、脆弱，现有测试只覆盖部分入口，拆分风险 > 收益，等页面结构稳定后再动 |
| 8 | `claimCard` 多策略合并 | 每个策略针对真实存在的不同卡片类型，是有意为之的兼容层 |
| 9 | DAPI 区域参数统一（部分硬编码 `cn`） | 疑似端点只对国区开放，行为已通过 `_getMeInfo(..., "cn")` 保持原样 |
| 10 | `reportDailyActivity` / 前台 DOM 双轨处理器合并 | 架构使然（前台只能点 DOM），已有防重复守卫 |

---

## 三、需要你手动处理的事项（不改代码）

### ⚠️ `.storage.json` 含真实凭证

`微软积分商城签到（全能智能重构版）.storage.json` 存有微软账号的 **refresh_token**（有效期最长 90 天）和 **OAuth 授权码**，持有者可操作你的 Rewards 账号：

- **不要**把它提交到任何 git 仓库或发送给他人；
- 若已外泄，去 account.microsoft.com 改一次密码即可使旧 token 失效。

### 🗑 可选清理

- `.bak` 备份文件（v3.3 左右，3311 行）确认新版稳定后可删除。

---

## 四、修复过程记录（第二轮 bug）

**「完成后停止循环」菜单提示反转**：原代码 `alert(!keep ? "已开启…" : "已关闭…")`，而切换后的新状态是 `!keep`，提示却反着判断：

| 点击前 keep | 点击后 Config.keep | 实际发生 | 原提示（错误） |
|---|---|---|---|
| `true`（继续循环） | `false`（停止循环） | **开启**了停止循环 | "已关闭…" ❌ |
| `false`（停止循环） | `true`（继续循环） | **关闭**了停止循环 | "已开启…" ❌ |

修复后条件改为 `keep`（即切换后的"停止循环"状态），提示与结果一致。验证方式：菜单命令回调在测试 harness 中不可达，已通过代码审查确认。

---

## 五、如何验证

```bash
node --test tests/userscript.logic.test.cjs   # 62/62 通过
node --check 微软积分商城签到（全能智能重构版）.user.js   # 语法 OK
```

在 ScriptCat 中重新安装 v3.5.1 后观察：单轮内同一接口只请求一次（GM 日志可见 `getuserinfo` 命中日志只出现一次）；执行后复查仍能看到刷新数据。
