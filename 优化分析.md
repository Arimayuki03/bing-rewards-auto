# 微软积分商城签到脚本 — 优化分析

> 分析对象：`微软积分商城签到（全能智能重构版）.user.js`（v3.4.0，3674 行，ScriptCat 用户脚本）
> 分析日期：2026-08-10
> 基线：`tests/userscript.logic.test.cjs` 27 个测试全部通过

---

## 一、现状评估（做得好的部分，不动）

- **架构分层清晰**：`Utils`（工具）→ `API`（接口）→ `TaskManager`（任务编排）→ 前台 DOM 处理器，职责分明。
- **测试基础扎实**：27 个逻辑测试覆盖了 token 刷新、卡片领取复核、搜索配额解析、运行锁等核心路径。
- **状态管理稳健**：跨实例运行锁、按日期隔离的状态键、跨午夜统一用运行起始日（`dateNowNum`）、"连续 N 次未确认即放弃"机制都经过深思熟虑并有注释说明。
- **防风控意识到位**：随机延迟、随机 UA、搜索间隔抖动均已实现。

以下优化按优先级排列：**P0** = 正确性缺陷，**P1** = 性能（请求量），**P2** = 代码质量，**P3** = 安全建议（不修改代码）。

---

## 二、P0 正确性缺陷

### P0-1 前台打卡/详情页处理器使用即时日期，与后台存储键竞态

**位置**：`clickDetailTasks()`（约 L3171）、`clickPunchCards()`（约 L3250）

两处均使用 `Utils.getTodayNum()`（即时日期）作为"今日已处理"判断键，而后台 `TaskManager` 统一使用运行起始日 `state.dateNowNum`。

**失败场景**：后台实例在 23:59:50 启动（`dateNowNum=今天`），一轮任务跨过午夜。前台此时打开 rewards 页面，`getTodayNum()` 已返回"明天"，与存储中的旧日期键不匹配，**打卡点击/详情页点击会被重复执行一遍**，产生不必要的重复上报（恰是防风控要避免的）。

**修复**：前台处理器改用 `RewardsAuto.state.dateNowNum`（页面加载时初始化为当天，语义与原逻辑一致；仅当页面跨午夜仍开着且后台旧实例仍在跑时，才与后台对齐为同一天——这正是要的行为）。

### P0-2 详情页"未找到按钮"即标记当日已完成，页面加载不全时永久跳过

**位置**：`clickDetailTasks()`（约 L3211-3219）

```js
if (clickable.length === 0) {
    ...
    GM_setValue(detailDateKey, today);
    GM_setValue(detailDoneKey, true);   // ← 问题
    return true;
}
```

**失败场景**：脚本在 `document-start` 注入，延迟 3 秒后查询按钮。若页面渲染慢/网络卡顿，按钮尚未出现就被判定"全部完成"并写入完成标记，**当天永远不会再重试**。对照：`autoClaimPoints` 对同样情况会重试最多 5 轮，这里没有。

**修复**：找不到按钮时只记日志、不写完成标记。`state>=` 守卫 + 每日尝试上限（`punchCardAttempts` 机制在打卡侧已有）足以防止无限重复。

---

## 三、P1 性能优化（单轮请求量约降 35-40%）

脚本每 20 分钟由 crontab 触发一轮完整流程。一轮非空闲运行中，同一个页面/接口被反复抓取：

| 资源 | 单轮抓取次数（现状） | 涉及调用方 |
|---|---|---|
| `rewards.bing.com/earn`（~1-2 MB 页面） | **最多 5 次** | `getRewardsInfo`（doSearch 首尾各一次 + runAll 汇总再一次）、`discoverCards`（doPromos 扫描+复核、二次扫描+复核、doStreak） |
| `rewards.bing.com/dashboard` | **最多 5 次** | `doPunchCard`、`_extractDailySetUrls`、`_extractDailySetHashes`（RSC 变体）、`doClaimPoints` |
| `api/getuserinfo` | 最多 3 次 | `getDailySetItems`、`getSearchQuotaFromUserInfo`、`discoverCards` 方法0 |
| DAPI `/me?options=613` | 最多 4 次 | `getReadProgress`（doRead 三次 + getRewardsInfo 一次）、`getSearchQuotaFromAPI` |
| DAPI `/me?options=105` | 2 次 | `getBalance`（runAll 首尾） |

这些接口在几分钟内不会变化（脚本自己的轮询间隔就是 20 分钟），重复抓取纯属浪费，且请求量本身就是风控信号。

### P1-1 引入"按运行周期"的页面缓存（核心优化）

新增 `Utils.fetchPage(url, opts)` / `Utils.fetchJson(url, opts)`：

- 以 `url + 变体标记 + dateNowNum` 为键，**一轮运行内**同一 URL 只请求一次，后续命中内存缓存；
- 跨轮次自动失效（日期键变化 + 每个 crontab 实例是全新进程）；
- 提供 `{ fresh: true }` 逃生门，供"执行后复查"类调用强制刷新（见 P1-2）；
- `getRewardsToken`（RVT token）已有 30 分钟缓存，不受影响。

**语义保证**：缓存只作用于"读取状态"的 GET 请求；所有写操作（reportActivity、Server Action、搜索上报）不经缓存。页面内容在一轮内不变正是脚本现有假设（`doPromos` 的复核逻辑就建立在"刚上报的会立刻反映在页面上"之上，复核路径已用 fresh 绕过）。

### P1-2 汇总复查强制刷新

`runAll` 末尾用于生成汇总通知的 `getRewardsInfo()` 改为 `getRewardsInfo({ fresh: true })`——汇总必须反映本轮执行后的最新进度，不能吃缓存。

### P1-3 DAPI `/me?options=613` 响应合并

`getReadProgress`、`getSearchQuotaFromAPI` 请求同一个端点，`getBalance` 请求的 `options=105` 是其子集。统一为一个带缓存的 `_getMeInfo()`：阅读进度、搜索配额、余额全部从同一份响应里取。一轮运行中该端点从 4-5 次降到 **1 次**。

### P1-4 移除 runAll 中 doDailySet 前的双份随机延迟

L3022-3023 连续两个 `randomDelay()`（一个在日志后、一个在任务前），删掉多余的一个。每轮省 3-8 秒且不影响防风控（任务前仍保留一份延迟）。

### 预期收益

典型非空闲轮（搜索为主要负载）请求数：**约 47 → 约 29**（-38%）。earn 页面每轮少传 4-8 MB。

---

## 四、P2 代码质量

### P2-1 数组洗牌改用 Fisher–Yates

**位置**：`getHotSearchWord()`（约 L2013）

`wordList.sort(() => Math.random() - 0.5)` 分布有偏（各排列概率不均），且 O(n log n)。改为标准 Fisher–Yates（`Utils.shuffle`）。搜索词顺序对功能无影响，纯质量改进。

### P2-2 日期工具统一

`getTodayNum` / `getTodayStr` 各自 `new Date()` 并重复格式化逻辑，抽成共用的 `dateParts(date)`。行为不变，减少一处将来改格式要改两处的隐患。

### P2-3 测试补强

为新增缓存层补测试：

1. 同一轮内重复调用 `getSearchQuotaFromUserInfo` / `discoverCards`，`getuserinfo` 仅请求一次；
2. earn 页面在一轮内仅请求一次（getRewardsInfo 两次调用 → xhr 一次）；
3. `{ fresh: true }` 绕过缓存；
4. DAPI `/me` 在 getReadProgress 与 getSearchQuotaFromAPI 之间复用。

### 评估过但不做的项（及原因）

| 项 | 原因 |
|---|---|
| `getRewardsInfo` 300+ 行解析逻辑拆分 | 解析路径多且脆弱（正则/括号匹配各有历史原因），现有测试只覆盖部分入口，拆分风险大于收益 |
| `claimCard` 多策略合并 | 每个策略针对真实存在的不同卡片类型，是有意为之的兼容层 |
| `_dapiRequest` 区域参数不一致（部分调用硬编码 `cn`，部分走 `_resolveRegion`） | 疑似有意（某些端点只对国区开放），无真实账号验证前不动 |
| 前台处理器与后台任务的 DOM/请求双轨 | 架构使然（前台只能点 DOM），且已有防重复机制（`punchCardDetailDate` 等守卫） |
| `checkRegion` 递归重试改循环 | 深度上限 2，无栈风险，可读性相当 |

---

## 五、P3 安全建议（不改代码，需要你手动处理）

### ⚠️ P3-1 `.storage.json` 包含真实有效凭证，请勿外传

`微软积分商城签到（全能智能重构版）.storage.json` 中存有：

- `Config.code`：完整的 OAuth 授权码 URL
- `Config.token`：微软账号的 **refresh_token**（有效期最长 90 天，持有者可静默换取 access token 操作你的 Rewards 账号）

**建议**：不要把这个文件提交到任何 git 仓库或发给他人；如需分享项目，先清空该文件。如果它已经外泄过，去 account.microsoft.com 改一次密码即可使旧 token 失效。

### P3-2 第三方热搜 API 的搜索词未二次校验

`getHotSearchWord` 会把第三方接口返回的热搜标题直接作为 Bing 搜索词。内容只发往 bing.com、风险低，但若要更稳，可加长度/字符白名单过滤。暂不改（会误伤正常词条，收益小）。

### P3-3 硬编码的 `next-action` 兜底值

Server Action 的兜底 hash（`70babb…`）是 Next.js 构建产物，微软重新部署后会失效。脚本已有动态提取 + 多策略兜底，无需处理，仅备案。

---

## 六、实施计划与状态

| # | 项 | 状态 |
|---|---|---|
| 1 | P1-1 `Utils.fetchPage`/`fetchJson` 按运行周期缓存 + 全部读路径接入 | ✅ 已完成 |
| 2 | P1-2 `getRewardsInfo({ fresh })`，runAll 汇总用 fresh | ✅ 已完成 |
| 3 | P1-3 DAPI `/me?options=613` 合并为 `_getMeInfo()` | ✅ 已完成 |
| 4 | P1-4 删除 runAll 多余延迟 | ✅ 已完成 |
| 5 | P0-1 前台处理器改用 `state.dateNowNum` | ✅ 已完成 |
| 6 | P0-2 详情页找不到按钮不再标记完成 | ✅ 已完成 |
| 7 | P2-1 Fisher–Yates 洗牌 | ✅ 已完成 |
| 8 | P2-2 日期工具统一 | ✅ 已完成 |
| 9 | P2-3 缓存层测试 | ✅ 已完成 |
| 10 | 版本号 3.4.0 → 3.5.0 | ✅ 已完成 |

验收标准：原 27 个测试 + 新增测试全部通过；行为语义不变（写路径无缓存、复查路径强制刷新）。
